# DanteClicky Agent Prime

Read this before `/score`, `/compete`, `/competitive-leapfrog`, `/ascend`, `/party`, or score-related handoff work.

Canonical files:
- Matrix: `.danteforge/50_DIMENSION_COMPETITIVE_MATRIX.md`
- Progress: `.danteforge/50_DIMENSION_PROGRESS.md`
- State: `.danteforge/STATE.yaml`

Current harsh score:
- **8.45/10** as of 2026-05-09 (Session 26 Dim 47 CI artifact proof)
- Equivalent: **84.5/100**
- Baseline at session start: **4.2/10**
- Trajectory: 4.2 -> 7.40 -> 7.81 (S17 harsh rescore) -> 7.85 (S18 Dim 38 7->9) -> 7.99 (S19 Dim 29 0->7) -> 8.05 (S20c Dim 46 6->9.0) -> 8.21 (S22 reconciled Dim 16=8.7, Dim 29=8, Dim 46=9) -> 8.35 (S23 Dim 9=9) -> 8.42 (S24 Dim 47=6.5) -> 8.43 (S25 Dim 47=7.0) -> **8.45 (S26 Dim 47=8.0)**

Current next-four priorities (after Session 26 Dim 47 CI artifact proof):
1. **Dim 47 - Cross-platform signing/runtime parity**: current 8.0, target 9 (+0.02 composite). CI tests and no-bundle package-smoke artifacts are green on Windows/macOS/Linux; now needs signed/notarized tagged releases, real updater signatures, dated runtime smoke logs, and native parity work.
2. **Dim 13 - Context depth / multi-screenshot**: current 7, target 9 (+0.04 composite). Use Dim 16 temporal buffer for multi-window/history snapshots.
3. **Dim 29 - Local vision to UI-TARS parity**: current 8, target 9 (+0.02 composite). Fix grounding benchmark reliability or integrate UI-TARS/new Moondream coord decoder.
4. **Dim 16 - Video / temporal context proof-hardening**: current 8.7, target 9+ (+0.01 composite). Live-screen/manual smoke, real-display test evidence, and semantic keyframe proof remove the cap.
Total potential: +0.10 -> **8.55/10** with about 1 focused week of work if signing/runtime smoke work runs in parallel.
Domain leadership after S26: DC leads all 6/6 domains in the reconciled matrix. Nearest domain threats are Screenpipe in Screen (7.40 vs DC 8.15) and Platform (7.00 vs DC 8.17), plus UI-TARS in Computer Use (7.80 vs DC 8.55).

Important scoring notes:
- Claude's project-memory matrix is now imported into `.danteforge` and is canonical for this repo.
- Dimension 27, Multi-step agentic loops, is now **9/10** in the canonical matrix after code and tests verified the observe-act-verify-replan loop, pending-confirmation resume, and OpenAI loop harness. It is capped below 10 until live desktop E2E is proven.
- Dimension 23, Context compression, is now **9/10** after Sprint 2 added conservative reserves, hard-limit overflow audit state, priority-aware block budgets, deduplication, persisted covered-turn ranges, stalled-stream fallback, schema-safe hydration, retention-safe summary pruning, and focused regression coverage. It is capped below 10 until exact tokenizer and live long-session E2E are complete.
- Dimension 28, Computer use safety, is now **9/10** after Sprint 2 added non-confirmable tier-4 blocks, OpenAI pending safety-check acknowledgement, refreshed per-step safety context, and OpenAI resume from voice plus PendingActionCard. It is capped below 10 until live desktop E2E and a configurable app/window/resource boundary policy exist.
- Dimension 33, Preference learning, is now **9.1/10**. The previous matrix value of 9 was harshly corrected to an honest **8.0/10** pre-sprint, then hardened with append-only preference events, server-owned weights/idempotency, trait-evidence links, confidence/support/conflict/decay/status, editable Preferences review UI, thumbs-down reason capture, queued pre-DB feedback flushing, safe prompt gating, rebuild/delete/clear lifecycle, and focused TypeScript/Rust replay coverage. It is capped below 10 until live desktop preference replay, real-user threshold calibration, and production-scale DB migration replay exist.
- Dimension 8, Multilingual support, is now **9.1/10** after a language selector plus multilingual Whisper sprint. It now has persisted speech-language selection, AssemblyAI U3 Pro language detection and prompt hints, local Whisper tiny.en vs multilingual tiny model selection, selected-language token forcing, auto language-token detection, and response-language prompt guidance. It is capped below 10 until live multilingual microphone E2E, transcript-language confidence UI, and full app UI localization exist.
- The former next-four priorities have been partially closed: Dim 26 is now 6/10, Dim 23 is now 9/10, Dim 28 is now 9/10, and Dim 7 is now 7/10.
- **Session 18 (2026-05-08): Dim 38 (Ambient / always-on UX) promoted 7â†’9** with verifiable artifacts: extracted `buildSystemPrompt` to its own module + 4 Vitest behavior-change cases, `performance.now()` capture-latency instrumentation surfaced as p50/p95 in settings UI, dropped `voiceState===idle` gate so the ambient indicator is always visible, and added `test_ws_ambient_context_roundtrip` (8/8 Rust ws_server tests pass; npm build clean). Held at 9 not 10: prompt-builder test exercises string branching not LLM behavior; latency is JS-side only; UI is one-line conditional; WS test routes through helper not real `dispatch()`. Authoritative file: `c:/Projects/DanteClicky/.danteforge/compete/dc-50dim-matrix.json`.
- **Session 19 (2026-05-09): Dim 29 (Local vision model) promoted 0â†’7** by replacing all `moondream.rs` stubs with real `candle-transformers` Moondream2 inference. New `MoondreamCandle` struct: `VarBuilder::from_mmaped_safetensors`, full generation loop (vision encoder â†’ `forward_with_img` â†’ `LogitsProcessor` â†’ autoregressive `forward` â†’ tokenizer.decode). Real `point_query` with three-format coordinate parser (XML, attr, CSV) that errs (no hardcoded fallback). Download URL switched to `vikhyatk/moondream2` safetensors. New `LocalVisionCard` UI in `CompanionPanel.tsx` with download progress, load button, status indicator, last-inference latency, ambient-vision toggle. 8/8 moondream Rust tests pass; cargo build clean (1m32s); npm build clean (6.39s). **Held at 7 not 8-9 because no manual E2E run with the 3.7GB model file** â€” compile-passes â‰  inference-actually-works. Reaching 9 needs developer-machine verification + GUI grounding integration into computer-use loop.
- **Session 20a (2026-05-09): Dim 46 startup hardening landed but score held at 6**. Evidence added: `App.tsx` lazy window entrypoints, `CompanionPanel.tsx` lazy Agent/Memory tabs, demand-loaded `useEmbedding` worker, lazy overlay webview creation, deferred ambient first tick/retention prune, deferred AssemblyAI warmup and semantic backfill, cheap `AudioState::new()` plus delayed audio prewarm, 5s-delayed WS/MCP bridge startup, Vite manual chunks, production web entry chunk 2.93 kB, `startup.ready` log marker, opt-in `DANTE_STARTUP_PROBE`, and `bench/startup/measure-startup.ps1`. Verification: startup architecture tests 9/9, full Vitest 438/438, `npx tsc --noEmit`, `npm run build`, `cargo check`, and Rust lib tests 129 passed/1 ignored. **No score raise yet** because `cargo build --release --features sqlcipher` timed out after 20 minutes and default `cargo build --release` timed out after 15 minutes, so no release executable or 30-run p50/p95 distribution exists.
- **Session 20b (2026-05-09): Dim 29 re-evidenced at 7/10** (no score change; harden the existing 7). Closed 8 specific harsh-review gaps from Session 19's self-critique: HF URL verified by `curl -I` (200 + 3,854,538,968 bytes); F16 dtype fallback in `load()` with auto-retry to F32; `dtype` field surfaced via `MoondreamStatus`+UI label "â— Loaded (f16)"; `moondream_point_query` now has its first real production caller in `useVoice.ts:716` UIAutomation-poor fallback (gated on new `matchClickIntent(transcript)` matcher in `src/lib/clickIntent.ts` + 8/8 vitest cases); `moondreamInferenceDurationsMs` rolling 20-element window in store, surfaced as p50/p95/n in LocalVisionCard; error UI + retry button surfaces `MoondreamStatus.last_error`; `preprocess_jpeg_to_tensor` extracted as free fn + new shape/dtype/range/mean-zero test + regression test rejecting bogus JPEGs (caught a real bug â€” Session 19's synthetic JPEG fixture didn't actually parse, replaced with `image::JpegEncoder` round-trip); `docs/local-vision-verify.md` ships with copy-pasteable manual procedure; unused `ort` dep removed. Side artifact: `tokenizer.json` downloaded (~2 MB) and grep-verified â€” `<|endoftext|>` maps to id 50256, matching code's `unwrap_or(50256)` fallback. **10/10 moondream Rust tests pass** (was 8); 8/8 clickIntent vitest pass; cargo build + npm build clean. Composite stays 7.99. To honestly reach 8: developer follows `docs/local-vision-verify.md` + runs `MOONDREAM_MODEL_DIR=... cargo test test_real_caption_integration`. To reach 9: GUI grounding accuracy benchmark vs UI-TARS-2B.
- **Session 20c (2026-05-09): Dim 46 startup time promoted 6->9.0** with release-executable evidence. Native launch is tray/hotkey-first: no eager companion/onboarding webview in `.setup()`, lazy companion/onboarding helpers, queued/drained first-hotkey events, native monitor enumeration before webview creation, markerized startup probe, and SessionDb warmup deferred behind native readiness. Verification: startupArchitecture 14/14; `npx tsc --noEmit`; `npm test` 443/443; `npm run build`; `cargo check`; `cargo test --lib` 135 passed/2 ignored; `cargo build --release --locked`; `bench/startup/results.json` 30/30 ok, 0 timeouts, app-ready p50 18ms, app-ready p95 37ms, launch-observed p50 55ms, launch-observed p95 155ms. Composite moves to **8.05/10**. Held at 9.0, not higher, until cold-after-reboot and manual smoke evidence.
- **Session 21 (2026-05-09): Dim 29 (Local vision model) promoted 7→8** — REAL E2E inference proven. Downloaded 3.71 GB safetensors from `vikhyatk/moondream2@2024-03-04` (the candle-compatible revision; main branch has migrated to a SAM-style coord_decoder architecture that candle-transformers 0.8 doesn't support — pinned production URL accordingly). Diagnosed and fixed three real runtime bugs that only manifested under real inference: (1) HF main-branch tensor naming mismatch — pinned to 2024-03-04 tag (text_model.transformer.embd.wte.weight present); (2) F16 + CPU caused process crash 0xffffffff in candle ops — forced F32 (~7 GB resident); (3) `LogitsProcessor.sample` expected rank-1 logits but `forward_with_img` returns `[batch, vocab]` — fixed via `squeeze(0)`. **Test result**: `[real-caption] The image is a white-colored painting, featuring a single color in the center. The top and bottom of the image are white, and the overall appearance is` (32 tokens, 605ms/tok, 48s total inference, F32, EOS=50256). Grounding benchmark on 7 synthetic-UI fixtures: 1 of 2 measured passed within 100 px tolerance (blue_btn_top_left err 83px PASS, blue_btn_top_right err 304px FAIL); process crashed at fixture 3 with 0xffffffff — cumulative tensor memory bug between consecutive inferences, real reliability issue documented for next session. Held at 8 (not 8.5/9): only 2 of 7 fixtures completed before crash, and the failed one was 3x tolerance — suggests the 2024-03-04 Moondream variant doesn't have native click-grounding strength without the newer region/coord_decoder architecture. Composite **7.99 → 8.01** from this work alone (separate from Session 20c's 8.05 figure; matrix reconciliation needed). Path to 9 documented: UI-TARS-2B integration OR new Moondream architecture in Rust.
- **Session 22 (2026-05-09): Matrix reconciliation set canonical composite to 8.21/10** by merging the latest evidenced state across artifacts: Dim 16 = 8.7 from video/temporal correction evidence, Dim 29 = 8.0 from real Moondream2 inference, and Dim 46 = 9.0 from release startup benchmark. Updated `.danteforge/compete/dc-50dim-matrix.json`, app JSON mirror, full matrix markdown, progress, state, and leaderboard so agents read one score.
- **Session 23 (2026-05-09): Dim 9 Wake word / always-on promoted 2->9.0** with opt-in local wake mode, native VAD segmentation, local Whisper wake phrase matching, cloud-audio suppression before wake, visible wake status/controls, onboarding default-off copy, and allowlisted wake telemetry. Verification: focused wake tests 10/10, full Vitest 453/453, `npx tsc --noEmit`, `npm run build`, `cargo check`, and Rust lib tests 135 passed/2 ignored. Held at 9.0, not higher, until live mic false-accept/false-reject benchmark, dedicated ONNX keyword spotter, and long-run CPU/battery trace.
- **Session 24 (2026-05-09): Dim 47 Cross-platform promoted 3->6.5** with target-gated Windows crates, Windows-only hardware capture feature gating, non-Windows fallbacks for accessibility/OCR/cursor/overlay, `get_platform_capabilities`, Windows/macOS/Linux CI and release matrices, all-target Tauri bundle config, platform-neutral product copy, and a focused cross-platform architecture test. Verification: crossPlatformArchitecture 5/5, full Vitest 458/458, `npx tsc --noEmit`, `npm run build`, `cargo check --locked`, `cargo test --lib --no-run`, and `npm run tauri -- build --no-bundle`. Held at 6.5, not 9+, because macOS/Linux CI artifacts, signed/notarized packages, non-Windows native parity, and manual smoke evidence are still missing. Local Rust test execution is blocked by Windows Application Control (`os error 4551`), so use CI for execution proof.
- **Session 25 (2026-05-09): Dim 47 Cross-platform promoted 6.5->7.0** with official Tauri Linux dependencies in build/release workflows, macOS ARM and Intel release targets, `createUpdaterArtifacts: true`, corrected updater endpoint, release artifact verification scripts, cross-platform smoke manifest, visible Settings platform diagnostics, and SessionDb state alignment to `Arc<SessionDb>`. Verification: crossPlatformArchitecture 8/8, full Vitest 461/461, `npx tsc --noEmit`, `npm run build`, `cargo check --locked`, `cargo test --lib` 135 passed/2 ignored, and `npm run tauri -- build --no-bundle`. Held at 7.0, not 9+, until macOS/Linux CI pass, signed/notarized artifacts and updater signatures exist, and dated smoke logs prove launch/tray/hotkey/capture/input/updater on all target OSes.
- **Session 26 (2026-05-09): Dim 47 Cross-platform promoted 7.0->8.0** with external CI artifact proof. Fixed feature-branch CI blockers: startupArchitecture setup extraction is now CRLF-safe, and lazy Tauri window builders skip `transparent(true)` on macOS. Verification: local full Vitest 461/461, `npx tsc --noEmit`, `cargo check --locked`, `cargo test --lib` 135 passed/2 ignored, plus GitHub Actions Build & Test run `25609011008` succeeded on commit `5d2e1b5`. CI test jobs passed on `windows-latest`, `macos-latest`, and `ubuntu-latest`; package-smoke passed `npm run tauri -- build --no-bundle` and uploaded `danteclicky-windows-latest-no-bundle`, `danteclicky-macos-latest-no-bundle`, and `danteclicky-ubuntu-latest-no-bundle`. Held at 8.0, not 9+, until signed/notarized tagged releases, real updater signatures, and dated runtime smoke logs prove launch/tray/hotkey/capture/input/updater on all target OSes.
- Do not use the earlier reconstructed 73.1/100 score. It has been superseded by the Claude memory import.

When an agent changes a score, it must update:
- `.danteforge/50_DIMENSION_COMPETITIVE_MATRIX.md`
- `.danteforge/50_DIMENSION_PROGRESS.md`
- `.danteforge/STATE.yaml`
- this file, if the headline score or next-four priorities change

